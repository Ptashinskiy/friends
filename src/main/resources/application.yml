spring:

  #Database properties

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create

  datasource:
    driver-class-name: org.postgresql.Driver
    username: ${DATABASE_USERNAME}
    password: ${DATABASE_PASSWORD}
    url: ${DATABASE_URL}

#Server's port
server:
  port: 8080
